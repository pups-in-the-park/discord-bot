//! Component (button/select) handlers for the reports feature. One file each;
//! the feature `router` decides which fires based on the custom-id.

pub mod action_chosen;
pub mod action_select;
pub mod dismiss;
pub mod investigate;
